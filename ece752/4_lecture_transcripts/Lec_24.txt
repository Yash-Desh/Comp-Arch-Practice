Title: Lec_24
Source file: Lec_24.mp3
Detected language: en
------------------------------------------------------------

I'm some sort of questions okay where we left off last time is we talked about cache coherence and came up with this very simple uh protocol for a write back cache validate and validate protocol went through it saw that it gave us the correct result the problem with the validate and validate protocol is it's only going to let one cached copy at any time okay so I have N processors only one of them can have a copy in their cache multiple copies cannot exist even if you adjust that block is purely read only you aren't even writing into it okay and you cannot have multiple read only copies this actually ends up being a big deal in a real situation because there are many situations that you know you have your global variables or whatever it is you know that you want to have you know copies of uh privately in the caches read only copies and that does not cannot exist with a simple read protocol so what we're going to do now is break that valid state into two different states into an M state and a S state a M state that refers to a modified state which indicates that a block in the modified state that cache has a copy and it's dirty and in the S state it means it has a local copy and it is clean so it's the same as all the other copies it's the same as what's in memory dirty means it is different from what is it what it is in memory and so this protocol will allow multiple read copies in that state but only a single read write copy in that state which is what you want if you want if you're writing into a block you should have the only copy no one else should have a copy write into or even read it's just going to get the wrong value so let us see what this protocol and see what it works with the different states so let me say start off by the end I don't have the block or alternatively I have the block in the meta state and if there's a read for the block going across on the bus I don't do anything I just stay in that same state a little bit okay now if I have a copy of the block in the meta state or I don't have a copy of the block if the processor does a read okay then that read goes out on the bus you get a copy of the block you get a copy of the block and keep it in the shared state if the processor does a write you're going to allocate that block do a write allocate and allocate that block in the cache and write into that block and bring that block in a modified state if you have the block in shared state you are doing a read you keep that block in shared state processor doing a read okay if the you have the block in shared state and you see a bus read in the bus side again you just keep the block as is you don't do anything to it okay if you have the block in shared state and you do a write then that write goes out on the bus as a bus write and that bus write will invalidate copies in other caches okay we'll get to the bus writes in this diagram in a little while and you transition your block into the modified state you write into your local copy and now this is indicating that this data over here in this cache is different it's modified and I have the only local copy and all the other copies have been invalidated okay and finally if you see a bus write bus write means some other processor is trying to write into that block I have a shared copy okay I am going to invalidate my block so transition to the invalid state over here and you know in the process of transitioning in some you know situations if I am also going to give the common block to the requester you know I could also copy back that block onto the bus otherwise I could just get that transition and not do anything with the block it's not good alright you as a processor may choose to be the one supplying the block instead of memory yes that cache could be responsible for supplying the block instead of memory and again I mean you may ask why because the memory would be hundreds of cycles away and this would be tens of cycles away alright okay now if I have a block in the modified state if I do a read or a write I stick in the same state okay if I see a bus read some other processor is trying to read it I have the dirty copy memory does not have the correct copy so I must copy back that block so that the other processor cache can have it and in the in doing that I likely also update the memory the memory also get updated okay and then I but what I do is now previously when another processor was trying to read when another processor was trying to read I went from the valid state to the invalid state okay here if the other processor was trying to read so there's a bus read signal I copy back that block but I transition to a shared state where I can continue reading that copy of the block okay if there is a bus write which means some other processor is trying to write into that block again I copy that block so the other processor can get it probably write the memory at the same time and then transition to an invalid state okay if my modified state and I replace the block so I have a write back I copy back that block and transition to the invalid state okay and if I do not have the copy of the block or have it in invalid state if the processor doesn't write then I read the block and then put it in modified state and write questions about this again I point out this is fairly simplistic in actuality you know each one of these states will also have multiple substates to take care of the variety of the different cases they have okay so let us see now how this works same piece of code this processor zero did a load so it brought that block you know value memory was 500 you brought that block with value 500 in shared state okay then 500 went into a register R4 you checked that subtracted it and then you stored R4 so now you know processor zero had a cache hit so if you did a write it was in shared state you did a write so you carried out the bus write to invalidate others if any and you transition to the modified state this processor now does a load processor one does a load misses so it misses so it issues the bus read signal okay this processor has it in processor zero has it in modified it sees a bus read signal okay so it copies back the block and transitions to the shared state over here so now this block was copied back on the bus from where it ended up in memory and it also ended up in this other processor cache and this state was set to share all right this processor now writes into that block so it has the block in shared state it does a write into that block so it does a bus write that bus write is going to invalidate the block in processor zero and you transition to modified okay so here this processor you know updates the block value over here to 300 and the block in processor zero is okay but you are allowing multiple read copies that can actually be a huge use of functions questions yeah did they ever build caches where like the modifier is simply one that is moved from one to the other yes so so you know this is what I'm talking about is they have invalidates the protocol they also built they also built the processor they had a not invalidate an update protocol where you update the value of this cache okay and they didn't turn out to be in a sense you know it's a little more difficult to get right okay and then then one of the other things that is routinely done is what is called a cache to cache transfer okay rather than getting it from memory you get it from a different cache and what was the situation you were asking if the if the cache that current has it hasn't modified will it send it to another cache and say you know it's you own it now it's your job to write it back to memory so in the cache to cache transfer you would also transfer the own so modify this modified state okay in a sense what it is this is only a single copy and it's different from memory that is many times also called an own state okay so that cache has the ownership of it so you can have protocols that transfer the ownership to the other cache but typically typically what you do is most code if you are communicating a shared variable okay you are doing a read before you do the write if someone else has done a write okay so we talk about coherence and very simple bus broadcast structure that's only one part of the equation okay let's see what the other parts of the equation are same piece of code okay same piece of code and the difference between this piece of code the execution over here and the execution over here is that over here processor zero started executing this code after processor sorry you want to start executing this code this is time after processor zero but you have no control over the things that are going to execute and here is not the same two different threads if you were and the execution is a little staggered so there is a little overlap in time of this execution okay so let's walk through this example and see what's going to happen again processor zero of memory is 500 does the load brings a block in shared state 500 and then that value of accounts with value 500 is loaded into register output this processor does this load misses in the cache it now goes and you know does a bus read does a bus read this processor processor zero has the cache in shared state block in shared state sees a bus read and so it doesn't do anything it stays in shared state alright so now you have two copies of the block in shared state which is what we wanted we thought was a good thing alright this processor takes the value from R4 which is 400 and stores it into memory so it stores the value 400 overhead okay and it is cached and it is modified and it invalidates this block okay this processor read 400 from memory put it in register R4 and now it did the subtraction on register R4 and now it writes R4 back to memory and R4 is 400 so it writes 400 invalidates this cache and creates this modifiers you know sets the value of this cache with state modified okay and you know these are write back caches okay everything should be fine but coherence is working fine but the value is wrong and the reason why the value is wrong is these access to that account only one processor should have been allowed to get access to that account at any given time that access should be synchronized and it wasn't synchronized so what really happened is that access should have been set to atomic these transactions should not have been interleaved but that's exactly what happened okay and you know this is not necessarily a situation on a multi processor the same thing can happen on a multi program uniprocessor because you can stop this thread if you would in the middle of this update over here and execute this thread and you would get the wrong way so what you have to do is you have to synchronize access to this shared account so that only one thread can be updating it at any given time alright so you see this in a course like 537 okay so synchronization that's the second issue for shared memory you have to regulate access to shared data and in a course like 537 you learned about semaphores and monitors and other synchronization constructs okay and for the hardware purposes the basic hardware primitive is unlock and we will see how we do that and so you can have operations that are going to acquire the lock and release the lock and the critical section which is what you want only one processor to be executing at any given time you isolate the critical section with a acquire a lock and release a lock so you first acquire the lock and only after you acquire the lock can you be in this critical section and when you're done with this critical section you're going to release this lock okay so you know these you are going to basically interleave the acquired releases so if you have an acquire there can only be one acquire the second consecutive acquire that happens before a release somehow is going to fail and if it fails then the second processor is not going to be able to enter into this critical section okay so how do we implement this acquire and release functionality how do we implement implement the lock you have to have another primitive that the instruction set has to support okay and we can have you know so this has to basically be an atomic operation we'll talk about one atomic operation a test and set operation and different architectures at least support different atomic operations I'll briefly you know give you an idea what these are after we've done this test and set okay so this test and set is a single instruction okay single instruction means the whole thing executes you cannot interrupt it in the middle and this single instruction atomically does the following operations it reads a memory location where the lock is okay and the values in the memory location can only be zero by one only two values okay zero means the lock is free and you can get it one means the lock is not free it's busy means someone else has locked it so atomically you read the value in that memory location into register R1 and you store the value one into that memory location okay again I repeat this is an atomic operation okay so you are implementing that load and store as one operation rather than hey these are separate loads and stores and things can go happen this is only one operation it has to be done atomically nothing else can happen in between so what this test and set does is if this lock was initially free you know so what it does is if it reads the value zero then R1 gets zero and you store one into that memory location so if the lock is if you see R1 has zero it means you were able to acquire that lock okay if R1 has one memory location had one R1 became one which meant that someone else has locked and then you also write one into that memory location it doesn't change the value of the memory location because it was already one okay if the value of the memory location was zero you got zero you wrote a one you change the value of the memory location from zero to one so in a sense you are basically saying hey you've got the lock and no one else can that lock is busy no one else can get it okay so what does this acquire look like what this acquire looks like would be would be this piece of code okay test and set into R1 the lock and branch not equal to zero on the value of R1 go back to so if this test and set returned a zero into R1 this branch would fail and you would move on to the next instruction this branch would fail if this branch fails means you actually acquired the lock okay if R1 is one this branch condition succeeds so you go back over here and keep reading that memory location and seeing as long as the memory location is one you keep in this loop over here the moment that memory location becomes one you break out of this loop you acquire the lock and you set the value of one in that memory location any questions on the acquire what would this release look like store just a store of zero into that memory location I mean a store is one there's no need to be that's the term all right so now if you have processor zero and processor one over here processor zero does that acquire test and set it gets the lock it can enter the critical section processor one comes to try and do the acquire it cannot get the lock so it keeps repeatedly spinning in this acquire loop until processor one is done with this critical section at the end of this critical section it releases the lock so it stores zero into R1 at that point processor one can acquire that lock and enter the critical section okay so what are these other mechanisms I can just briefly overview them without getting into details you know swap is to basically atomically swap two values okay exchange is very similar to swap the values into different memory locations exchange is you know very similar to swap fetch and add is you actually read the value of a memory location and add some constant to it that also you can implement with that you know some atomic operations and compare and swap is you're going to compare to memory locations and if a condition is met then you swap so you know the some of these you know other mechanisms is like the fetch and add one of the niceties about the fetch and add typically is you know if you're trying to do work in parallel let's say you're trying to do you know different loop iterations in parallel okay and you know the and you're atomically trying to access the induction variable if you were okay to get a different value of the induction variable okay if you do a fetch and add operation you could have multiple process for doing a fetch and add and they will get a value of the induction variable I want to go into one more detail but you know I mean the test set is the one that is most commonly used okay but you know some of the more advanced scenarios I mean I think the N337P has swaps and exchanges as a basic primitive locking up synchronization I shouldn't say locking up Questions? Can you use fetch and add for making some reports? No fetch and add was more used for more like you know what a pre-tix operation, doing parallel pre-tix operations so basically you have a you have a counter let's say you have a counter and then you have n processors trying to get that counter each one wants to get a different value of that counter so if you do it that's in fact what's going to happen is one processor gets it other keeps spinning and that spinning means repeated memory accesses and then the next processor is going to keep spinning even longer the repeated memory accesses multiple processes repeated memory accesses so one follow on to these basic locks has been hey the router then had each pro and then what happens is once the lock gets released all the remaining processors fight for it that is again repeated activity and you get a hot spot if you get a hot spot at some memory bank because it has a lock then anything else that's happening in the network also freezes so one variation of the locks that people have proposed is queue based locks then what you do is you actually you know you try and get a lock if you can't get the lock you queue up so there's no queue of processors and now when you release a processor one release a lock it passes the lock to processor two so the other is going to wake up and aren't fighting to go get the lock again so you don't have that hot spot what the fetch and add says is look you know each processor wants to increment that variable by one so I have you know I have a large number of processors each trying to increment this variable by one and those requests will all go over in some network so what you can do is the process can be fetch and add and you know the network could be built so that these requests multiple fetch and add requests combine so this fetch and add plus one plus one becomes this fetch and add plus two this one becomes fetch and add plus two and then these two can combine and then I remember over here is when I get the value back I'll give one value to this one and that value plus one to this one over here and then this becomes fetch and add four so eventually only one request fetch and add plus n reaches this processor if this was x this becomes x plus n if you were and then you return this back and those values each one of these can end up getting the x plus i process rocking so that was the rationale behind fetch and add and they actually built combining networks to do this and what this happens now is these processes aren't repeatedly fighting trying to get this locked so this network the traffic in there is minimized and other requests could be going on here's the way you unblock it make sense thank you two questions so the first one is you can ask n so if we are doing spinlocks in one of the very first papers you had used for reading which was the ARM paper they had they claimed that they could detect spinlocks and they could power down their threads so how would that like how difficult is that you know I mean people like to make claims you know we are repeatedly spinning okay and you know sometimes the OS needs to get involved you know or they could do whatever it is do some of that obvious on that is people propose these key based locks it's better to in a sense avoid them rather than detect them yeah depends on if you have or take what's the other question the other one was you said about hotspots in the memory and this is more pertaining to what when you have warehouse scale computers they have bit ellipses because these are all in memory right so even if a bit flips right and secondly like bit flips are going to happen just because of the scale we have and if we are running old code that probably only uses test net set or something like that then how do large scale panel programs or even these warehouse systems actually suddenly even you are not using this kind of low level synchronization at that level you are using higher level software and it's doing something usually compared to exchanges it's doing something like this but certainly it's not all the repeated accesses etc. I mean to make this work across machines you need something like some really complicated protocol consensus is hard yeah life is good everything is sequential you don't have to interact with anyone then you control your own situation the real world unfortunately doesn't work that way there are others around that get in your way okay alright so we figured out synchronization that was the easy part can I ask a different question Blake we go all the way back to earlier in the class let's think about what's happening in the pipeline with this like test and set it's kind of complicated doesn't it it does you have to do the read of the cache okay you have to do the read of the cache and the write of the cache you know big atomically more importantly those then you have to do the coherence protocol on top of that alright so if they have to write flip it to a modified state then you have to invalidate all the other copies and then now when they go get their lock they are going to try and get read copies etc it gets complicated even inside the pipeline right you know we've been talking about some of this multi-threading stuff you can't another thread see that this value is being changed you've got to be snooping and watching what the another thread is doing if they're accessing okay did everybody hear that question so so where is the other thread so we just read the spark paper where they have these four threads running on the same core they're dispatching so they kind of have to see what's happening in that memory location and know that they can't use that value even if you know because the other thread is in the process of doing this type of thing so any of these things when we talked about the coherence protocol so far the simple coherence protocol that we were talking about we relied on the bus being a centralization point where everyone was going through and based on the arbitration protocol for that bus one process or one thread is going to get that ahead of the others and then the others see what this thread is alright so very similarly the cache is going to be that centralization point where whoever gets to whichever thread gets to the cache first with the best in fact won't get that long but there has to be that centralization point otherwise protocols will become complex as you were just talking about the same things you see in disappeared systems make sense? okay which then leads to the other issue where what memory coherence did was it created a globally uniform consistent view of a single memory location or a cache line it basically what we did is made sure that that a single cache line is updated in particular order based on the order of the processor access it and importantly everyone sees that same order so there is a globally uniform and consistent view at the cache line that unfortunately is not enough because two different cache lines can be individually consistent solely beautiful here but they can be inconsistent with respect to each other what does that mean? we will see very shortly and because that could be inconsistent with respect to each other we have to talk about memory consistency and memory consistency model which is going to create a globally uniform consistent view of all the memory locations relative to each other and by the way this stuff is complex okay it is complex even if you take a course like 757 it still is complex I'm just going to give you an introduction to what it is and the question is who cares? well anyone who writes a parallel program cares the simple solution is to not write parallel programs okay or then you actually have to restrict the creation to a small number of people who can actually understand the stuff who care about the stuff so some people don't care okay what is this? here is a very very simple piece of code I have two variables and two different cache blocks A and flag both are 0 okay what processor 0 does is it first sets A equal to 1 and it sets flag after that okay processor 1 is spinning on while not flag okay so while flag is 0 it is even on spinning okay flag is 1 it breaks out and executes the sprint instruction and print state so what value of A gets printed out? it could be 0 or 1 1! come on this is like a basic simple programming thing you know it should be 1 look very simple begin 1 and then flag begin 1 if flag is 0 A is not going to get printed out A is only going to get printed out if flag is 1 and if flag is 1 A is 1 A is 1 right? well that's what intuition says when you write the program yeah that's how this program behaves coherence says nothing about it why? why is that the case? so you know where's my multi processor? whatever this happens even in a one space multi processor you know I have multiple paths if I want to have values A go from here to here it could go down this way flag go from here to here it could go down this way okay so P1 can seize P0's right of flag before it sees the right of A how can that happen? because maybe this coherence meant of A got delayed somewhere in the network it took a different path to the network okay oh it got decided hey spring break for me I'll come back later and it came back after its own sweet time okay and then you can start saying hey maybe I can have networks that are ordered and this way and that way and so on like that but even if this process has a very very simple write buffer we know write buffers very simple thing okay if in the write buffer you either maybe merge requests or some other processor can go and see it in a different order okay you can lose the ordering of the read and write accesses and have this memory consistency so you know again this is a really frustrating stuff to get you know I gave you sort of the example you know parallelization bug in one of the Adobe tools didn't show up after having been working in real life for many years so what you have to do is you have to try and give some tools for the program or to say yeah if I do this I'm going to get the right result which I expect A equals one okay so that is what is called the memory consistency model and in the most basic the simplest model which is also the hardest to implement and also therefore has you know a significant you know performance impact is what is called sequential consistency so a memory consistency model is a formal definition of the memory view that a programmer has. And in sequential consistency each processor sees its own loads and stores in the program that we know how to do with our load buffers and store buffers so even if you're doing specular execution out of order execution and so on and so on like that each processor can see its loads and stores in program order. Each processor also so processor one needs to see processor zeroes loads and stores in that program okay what the reason why A got printed zero is because processor one saw a different order of these stores than the order in which processor zeroes the internet okay. And finally all the processors need to see the same global load and store and you know Leslie Lamport came up with this and basically you know this sequential ordering corresponds to some sequential interleaving of the uniprocessor orders. It doesn't have to be just one and only one it has to be one sequential ordering but everyone has to see that same sequential ordering so you know so it should be indistinguishable as if you had a multi-programmed uniprocessor and the different programs ran on the uniprocessor one after the other. That's how their loads and stores are interleaved that's how everyone saw the updates that those programs had done and these two are not easy to enforce naturally via especially when you have more complex networks where you can take different paths to the network and you know how to maintain order etc etc. How do we enforce sequential consistency? Again I'm giving you sort of a very simplistic view over here it can get to be quite complicated and you know what it takes to enforce sequential consistency is all loads and stores must be globally ordered and basically what this means is that hey another processor processor one never sees processor zero loads and stores what it sees is coherence events that were created by processor zero okay so you know the coherence events due to that result from all the loads and stores should be globally ordered. So let's ask ourselves when do these coherence events happen naturally? They happen when you're accessing the cache so you know for stores when do we access the cache on stores? When a store is retired and we are retiring stores in order okay so that seems to be good however what we further saw you know when we are writing doing stores in the cache we have this write buffer and as far as the processor is concerned the store is done in order to the write buffer from the write buffer how the coherence event happens so should the processor wait for the entire coherence event from that store to happen all the events and you know and which is something that you actually end up having to do to maintain sequences which can take quite you know a long time okay now you know for loads we figured out we have to do a more important process right otherwise you aren't going anywhere that's why we had dependence predictor, out of order loads and stores do whatever it is etc etc you know if I say no I cannot do out of order execution of loads and stores that just doesn't go anywhere but what you can do is even the processor should be doing its loads out of order you can further than especially for the loads that actually end up retiring not for the speculative ones make sure that they are seen by other processors in order as if they happen in order okay are you with me on this you know that is in a sense what you have to do but as we know play also suppress we don't have to do them in order we just have to get the appearance of having them in order so what you can further do is you know once you actually have built a processor with speculative execution and it will recover from a branch desperation it can recover from load being executed before a prior store to the same address no problem we don't want to do that we can in an additional exception case if you will is hey I saw a coherence event that was not what would have happened had the other processor seen my loads and store operations that is actually much more complicated than that so you can treat these out of order loads and stores as speculative and treat certain coherence events as some kind of mis-speculation so for example if you actually have a bus ride request to a block with a speculative load pending alright then that means some other processor is trying to write over there and I should this load that I am seeing you know out of order is a mis-speculation and I can recover from that I'm just saying this very simply it can actually get quite complex now so the initial consistency like I said over here is that all loads and stores have to be globally ordered alright and that requires me to make sure that all the other processors see my loads in the order and if not again maybe you know there are tricks I can play to do with speculation strong belief that this memory is the sequential consistency model which is making sure that all processors see all loads and stores in the same order is too restrictive for performance considerations okay because it just cannot you know implementing it and so on you know is it is too restrictive okay now why did we have this problem over here the problem we had over here was while A was a shared variable that this process of writing and this process of reading this flag is more like a synchronization variable okay so the problem we got was we were treating both these rights as if they were just you know ordinary we were not distinguishing synchronization operations from non-synchronization operations okay so what some of the weaker memory models okay do and there are a few weaker memory models you know total store order weak ordering and you see some of these discussions if you take a look at you know discussion risk five or discussions in the media or whatever it is about the memory ordering and people are going for weaker memory models okay the weaker memory memory models basically they say the following is you know if you want to write sort of a parallel program and do it correctly all your shared memory is the same separate out parts of the shared memory that you're using for synchronization from other parts of your memory that you're not using for synchronization okay and then the hardware enforces sequential consistency only for the synchronization variables okay what about the other okay for the others then I have to make sure that you create these things for barriers and when you have a barrier you make sure all memory operations before the barrier have completed globally before you can get past that barrier okay so the synchronization points end up being some barriers and you make sure all memory operations from your processor before you hit a barrier have completed globally before you can get past that barrier okay so in this case you know if this were the synchronization operation before I get past the synchronization operation I'm going to make sure that all the memory operations before this have completed globally so this right of A equal to 1 has reached everywhere okay all the coherence operations have completed okay and now you know this is the barrier and so when I get to this point I don't get to this point after that point and so therefore I'll repeat okay so you know what you have to do with those weaker memory models is basically separate out the the separate out accesses operations on synchronization variables you can reserve a certain of memory for synchronization variables separate the operations on identify the operations on the synchronization variables separate them from non-synchronization operations okay that's easier said than done okay because you know this release operation is just a normal store so now I have a normal store different from synchronization area of memory and non-synchronization area of memory and so you know people write that program it sometimes work it doesn't work if they didn't properly synchronize the program identify with the appropriate barriers and you know there's still the debate is hey am I necessarily getting that much more the issue with memory consistency the weaker memory model is you get more performance but then the sequential consistency implementations come up with more cleverness and you know speculation and so on like that that can perhaps get better performance than the weaker memory models but as good at least so people are still fighting that anytime you have a new RSA you know for that weaker memory model is pushing results from the sometimes they're right so okay so question like today in Linux there's memory barriers that are in the kernel but there's no special memory area for synchronization so who who's playing this game with you know special memory areas uh uh uh so so actually let me not say necessarily special memory areas separately if you would basically what you're doing before there was properly labeled programs you specifically labeled you know labeled synchronization operations that work on some area of memory but then you don't write into that area of memory as part of your problem so the kernel is making these primitives and it's managing them okay that's what I mean it doesn't mean some program is fun but throw it at it but you don't do that you can probably try and do some production games and then be uh but yeah yeah so anyway so we talked about three aspects coherence, synchronization, consistency and you know you you know there is much much more to coherence than we talked about you know some more about synchronization than a lot more about consistency which you talk about in the course like so that's all that I have to say questions yeah like how processes from the same company supporting the same and relax their consistency models across their rings never so never because you know so you know I mean many of these processes some came up with something called total store order you have to the hardware implementation has to you know enforce that uh uh uh consistency model and total store order and others are you many others are using total store order it uses something very similar because the software created regardless of which it expects that's the memory consistency model Yeah yeah well the it has to go I want to seven seven so it's your I say what you're saying, I say what you're saying, see what you're saying is that, that you have your software written with a particular memory model for an architecture and now you want to weaken the memory model because that's what the newest processors are. You want to have the weaker memory model than your processor, you better support an hardware. So you have to support the weaker one and the way basically it means very simply is that if you wrote your programs on that your weaker memory model you can use that No amount of software routines and emulation at that base level, you know, okay, you know, what you could do, you could probably play games like, hey, detecting a But that's going to be so common that you probably want to provide that hardware support. You know, I mean, you know, you hear a lot, you hear a lot about, you know, I could write or retranslate binaries, I could, you know, recompile them, you know, create binaries and translate them into something like that. You know, if there's an error, you know, it's like, you know, one of the reasons why ideas basically, you know, and I don't see this happening, you know, companies would go down to Wall Street. Look, we have this, we have this.
